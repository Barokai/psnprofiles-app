import androidx.compose.runtime.Composable
import androidx.compose.material.Text

@Composable
actual fun UpdateProfileWebView(onFinished: () -> Unit) {
    Text("Update via WebView on iOS is pending.")
}
